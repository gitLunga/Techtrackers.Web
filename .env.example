# Base path for API calls. Left as a relative path so Vite's dev proxy handles
# it locally and the same build works when the API is served from one domain.
# Set to a full URL (e.g. https://api.example.com/api/v1) for a split deploy.
VITE_API_BASE_URL=/api/v1
VITE_SOCKET_URL=
